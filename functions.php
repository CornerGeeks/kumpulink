<?php

require_once("header.php");

/*
$tags should be a csv or perhaps an array? Use JSON?
*/
function add_bookmark($url,$title,$description,$tags){


	return $bookmark_id
}

/* 
should there be tag table? or should it just be text. Hmm..
Should look into best practices.
*/
function add_tags($bookmark_id,$tags){
	
	return true;
}

function add_tag($bookmark_id,$tag){
/*
if tag exists, assign tag_id.
else create new tag. Assign tag
*/
	return true;
}

